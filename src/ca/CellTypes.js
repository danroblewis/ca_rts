/**
 * Cell Type Constants
 * 
 * These match the constants defined in shaders/common/cell_types.glsl
 * Used when initializing the grid from JavaScript.
 */

export const CELL_EMPTY = 0;
export const CELL_RESOURCE = 1;
export const CELL_UNIT = 2;
export const CELL_OBSTACLE = 3;
